<h2>Criar Novo Post</h2>

<%= form_with model: @post, local: true do |form| %>
  <div class="form-group">
    <%= form.label :title, "Título" %>
    <%= form.text_field :title, class: "form-control", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :author, "Autor" %>
    <%= form.text_field :author, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :body, "Conteúdo" %>
    <%= form.text_area :body, class: "form-control", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :tag_names, "Tags (separadas por vírgula)" %>
    <%= form.text_field :tag_names, value: @post.tag_names || @post.tags.map(&:name).join(", "), class: "form-control" %>
  </div>

  <%= form.submit "Criar Post", class: "btn btn-primary mt-3" %>
<% end %>

<hr>

<h4>Upload de Arquivo TXT</h4>
<p>Você também pode criar múltiplos posts enviando um arquivo TXT no formato correto (Título | Nome | Conteúdo | Tags). </p>

<%= form_with url: upload_posts_path, multipart: true, local: true, method: :post do |form| %>
  <div class="form-group">
    <%= form.label :file, "Selecione o arquivo TXT" %>
    <%= form.file_field :file, class: "form-control", required: true %>
  </div>
  <%= form.submit "Enviar Arquivo", class: "btn btn-secondary mt-2" %>
<% end %>
